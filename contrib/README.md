# Contrib

This directory contains standalone utility scripts that are **not** part of the automated bootstrap flow or Nix/Home Manager configuration.

These scripts were moved from `share/` to declutter the core project structure.
