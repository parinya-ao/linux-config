# Python & FastAPI Skill
Focus on modern, type-safe Python and robust FastAPI development.

## Guidelines
- Always use Pydantic v2 for data validation.
- Use dependency injection for DB sessions and security.
- Prefer `async def` for I/O bound endpoints.
- Document all endpoints with OpenAPI descriptions.
- Use `TypedDict` or `dataclasses` for internal data structures.
